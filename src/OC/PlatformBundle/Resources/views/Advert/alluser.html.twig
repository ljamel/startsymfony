{# src/OC/PlatformBundle/Resources/view/Advert/view.html.twig #} {% extends "OCPlatformBundle::layout.html.twig" %} {% block title %} Lecture d'une annonce - {{ parent() }} {% endblock %} {% block ocplatform_body %} {% for user in users %}


<div class="nk-blog-post-border-bottom">
	<h3>
		<a href="{{ path('oc_platform_user', {'user': user.username}) }}"> <img class="avatarliste" src="{{ app.request.uri }}/../../images/{{user.username }}avatar.jpg" alt="{{user.username }}"> {{user.username }} </a>
	</h3>
</div>

{% endfor %}

<div class="nk-pagination nk-pagination-center">
	<nav>
		{% for p in range(1, nbPages) %} {% if p==page %}
		<a class="nk-pagination-current" href="{{ path('oc_platform_alluser', {'page': p}) }}">{{ p }}</a>
		{% else %}
		<a href="{{ path('oc_platform_alluser', {'page': p}) }}">{{ p }}</a>
		{% endif %} {% endfor %}
	</nav>
</div>

{% endblock %}
